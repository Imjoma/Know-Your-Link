import React from "react";

const MeetingPageLayout = ({ children }) => {
  return (
    <div className="max-w-screen-md mx-auto overflow-hidden ">{children}</div>
  );
};

export default MeetingPageLayout;
